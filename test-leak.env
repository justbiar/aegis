# INTENTIONAL TEST FIXTURE — not a real leak.
#
# This is a disposable Starknet Sepolia testnet key, deliberately committed
# here so Aegis's own scanner (and anyone reviewing this repo) can verify
# the detect -> derive-address -> check-balance -> rescue pipeline end to
# end, against a real key with no account address given anywhere in this
# file. Sepolia only. No mainnet funds, no real-world value.
PRIVATE_KEY=0x0079e05c37c5e5885311d4c38deebb0c55cbd1458c3ab6d6fd516a3c454ca6d0
